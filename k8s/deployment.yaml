apiVersion: apps/v1
kind: Deployment
metadata:
  name: name-application
  labels:
    app: name-application
spec:
  replicas: 2
  selector:
    matchLabels:
      app: name-application
  template:
    metadata:
      labels:
        app: name-application
    spec:
      containers:
        - name: name-application
          image: docker.io/raziriz/name-app:c01bb1ccc7cb5bd58e37f2d165372b52e1644d9b
          ports:
            - containerPort: 5000
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
